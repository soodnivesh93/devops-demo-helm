apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-demo.fullname" . }}-test-connection"
  labels:
    {{- include "devops-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
